/**
 * DATOS MOCK PARA UNIBE SOCIAL NETWORK
 * 
 * Este archivo contiene los datos iniciales de la aplicación.
 * Por defecto, está vacío para que solo aparezcan usuarios reales registrados.
 * 
 * Nota: En un entorno real, estos datos vendrían de una base de datos.
 */

import { User } from '../types';

/**
 * Lista de usuarios registrados en la plataforma
 * Inicialmente vacía - se llena cuando los usuarios se registran
 */
export const mockUsers: User[] = [];
